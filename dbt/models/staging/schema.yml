version: 2

models:
  - name: stg_orders
    columns:
      - name: order_id
        data_tests:
          - not_null
          - unique
      - name: customer_id
        data_tests:
          - not_null
          # Catches orphaned orders where customer was deleted without going through the CDC delete path.
          - relationships:
              to: source('default', 'customers_current')
              field: customer_id
      - name: status
        data_tests:
          - accepted_values:
              values: ['pending', 'confirmed', 'shipped', 'delivered', 'cancelled']
      - name: total_amount
        data_tests:
          - dbt_expectations.expect_column_values_to_be_between:
              min_value: 0
              max_value: "{{ var('max_order_amount', 100000) }}"
      - name: is_flash_sale_order
        data_tests:
          - accepted_values:
              values: [0, 1]
      - name: seconds_in_current_status
        data_tests:
          - dbt_expectations.expect_column_values_to_be_between:
              min_value: 0
              max_value: "{{ var('max_status_seconds', 86400) }}"
              config:
                severity: warn

  - name: stg_products
    columns:
      - name: product_id
        data_tests:
          - not_null
          - unique
      - name: sku
        data_tests:
          - not_null
          - unique
      - name: inventory_count
        data_tests:
          - dbt_expectations.expect_column_values_to_be_between:
              min_value: 0
              max_value: "{{ var('max_inventory', 10000) }}"
      - name: stock_status
        data_tests:
          - accepted_values:
              values: ['out_of_stock', 'low_stock', 'in_stock']
      - name: price
        data_tests:
          - dbt_expectations.expect_column_values_to_be_between:
              min_value: 0
              max_value: "{{ var('max_price', 10000) }}"
